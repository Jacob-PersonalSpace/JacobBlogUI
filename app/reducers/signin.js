// import { SIGNIN_POST, GOTO_SIGNUP } from '../actions/constants';

// const initialState = {
//     signin: {}
// };

const signInReducer = (state = 0, action) => {
    // switch (action.type) {
    //     case SIGNIN_POST:
    //         return Object.assign({}, state.signin, {
    //             status: action.status,
    //             message: action.response
    //         })
    //     case GOTO_SIGNUP:
    //         return Object.assign({}, state.signin, {})
    //     default:
    //         return state
    // }
    return state;
}

export default signInReducer;